spack:
  concretizer:
    unify: false
    reuse: false 
  view: false
  specs:
    - 'kynema~kynema_sgf_gpu~kynema_ugf_gpu~cuda'
    - 'kynema+kynema_sgf_gpu~kynema_ugf_gpu+cuda cuda_arch=90'
    - 'kynema+kynema_sgf_gpu+kynema_ugf_gpu+cuda cuda_arch=90'
