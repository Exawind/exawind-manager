spack:
  concretizer:
    unify: false
    reuse: false
  view: false
  specs:
  - exawind+fsi%oneapi ^openfast@develop
  modules:
    default:
      arch_folder: true
      roots:
        tcl: /projects/wind/modules
